# register.asm
# NASM CPU Registers Example

This is a NASM assembly program demonstrating:

- Use of all general-purpose registers (EAX, EBX, ECX, EDX, ESI, EDI)
- Arithmetic operations
- Conversion of integer to string
- Printing output using Linux syscalls

## How to run

1. Install NASM and a Linux environment (or WSL on Windows)
2. Assemble the code:
